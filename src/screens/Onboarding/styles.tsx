import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },

  titleList:{
    color:'#FFFF',
    fontWeight:'800', 
    fontSize:28, 
    marginBottom:10
  },

  dicriberList:{
    fontWeight:'300', 
    color:'#FFF'
  }
});
